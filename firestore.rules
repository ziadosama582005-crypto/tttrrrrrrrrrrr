rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // =====================================================
    // 🔐 قواعد أمان Firebase للمتجر
    // =====================================================
    // ⚠️ هذه القواعد للمرجع فقط!
    // يجب نسخها إلى Firebase Console > Firestore > Rules
    // =====================================================
    
    // ❌ القاعدة الافتراضية: رفض كل شيء
    match /{document=**} {
      allow read, write: if false;
    }
    
    // =====================================================
    // 1️⃣ المنتجات - Products
    // ✅ القراءة للجميع (عامة)
    // ❌ الكتابة محظورة (فقط من السيرفر)
    // =====================================================
    match /products/{productId} {
      allow read: if true;
      allow write: if false; // فقط من Server (Admin SDK)
    }
    
    // =====================================================
    // 2️⃣ الفئات - Categories
    // ✅ القراءة للجميع
    // ❌ الكتابة محظورة
    // =====================================================
    match /categories/{categoryId} {
      allow read: if true;
      allow write: if false;
    }
    
    // =====================================================
    // 3️⃣ المستخدمين - Users
    // ❌ القراءة والكتابة محظورة من العميل
    // فقط من السيرفر (لحماية الأرصدة والبيانات)
    // =====================================================
    match /users/{userId} {
      allow read: if false;
      allow write: if false;
    }
    
    // =====================================================
    // 4️⃣ الطلبات - Orders
    // ❌ القراءة والكتابة محظورة من العميل
    // =====================================================
    match /orders/{orderId} {
      allow read: if false;
      allow write: if false;
    }
    
    // =====================================================
    // 5️⃣ مفاتيح الشحن - Charge Keys
    // ❌ محظورة تماماً (حساسة جداً)
    // =====================================================
    match /charge_keys/{keyId} {
      allow read: if false;
      allow write: if false;
    }
    
    // =====================================================
    // 6️⃣ سجل الشحنات - Charge History
    // ❌ محظورة من العميل
    // =====================================================
    match /charge_history/{chargeId} {
      allow read: if false;
      allow write: if false;
    }
    
    // =====================================================
    // 7️⃣ الدفعات المعلقة - Pending Payments
    // ❌ محظورة تماماً (حساسة جداً)
    // =====================================================
    match /pending_payments/{paymentId} {
      allow read: if false;
      allow write: if false;
    }
    
    // =====================================================
    // 8️⃣ طلبات السحب - Withdrawal Requests
    // ❌ محظورة من العميل
    // =====================================================
    match /withdrawal_requests/{withdrawalId} {
      allow read: if false;
      allow write: if false;
    }
    
    // =====================================================
    // 9️⃣ الفواتير - Invoices
    // ❌ محظورة من العميل
    // =====================================================
    match /invoices/{invoiceId} {
      allow read: if false;
      allow write: if false;
    }
    
    match /merchant_invoices/{invoiceId} {
      allow read: if false;
      allow write: if false;
    }
    
    // =====================================================
    // 🔟 الإعدادات - Settings
    // ✅ القراءة للجميع (للهيدر والإعدادات العامة)
    // ❌ الكتابة محظورة
    // =====================================================
    match /settings/{settingId} {
      allow read: if true;
      allow write: if false;
    }
    
    // =====================================================
    // 1️⃣1️⃣ سجلات الأمان - Security Logs
    // ❌ محظورة تماماً
    // =====================================================
    match /security_logs/{logId} {
      allow read: if false;
      allow write: if false;
    }
    
    // =====================================================
    // 1️⃣2️⃣ السلات - Carts
    // ❌ محظورة من العميل (يُدار من السيرفر)
    // =====================================================
    match /carts/{cartId} {
      allow read: if false;
      allow write: if false;
    }
    
    // =====================================================
    // 1️⃣3️⃣ إحصائيات السلة - Cart Stats
    // ✅ القراءة للجميع (إحصائيات عامة)
    // ❌ الكتابة محظورة
    // =====================================================
    match /cart_stats/{statId} {
      allow read: if true;
      allow write: if false;
    }
    
    // =====================================================
    // 1️⃣4️⃣ المشرفين - Admins
    // ❌ محظورة تماماً
    // =====================================================
    match /admins/{adminId} {
      allow read: if false;
      allow write: if false;
    }
    
  }
}
